{
  "name": "auth-edge",
  "version": "1.0.0",
  "type": "module",
  "dependencies": {
    "aws-jwt-verify": "^4.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "dependencies": { "aws-jwt-verify": "^4.0.0" },
  "scripts": {
    "build": "esbuild index.mjs --bundle --minify --platform=node --target=node18 --format=cjs --outfile=dist/index.js"
  }
}
