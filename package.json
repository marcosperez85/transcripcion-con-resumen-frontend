{
  "name": "transcripcion-con-resumen",
  "private": true,
  "workspaces": ["frontend"],
  "scripts": {
    "build:frontend": "npm -w frontend ci && npm -w frontend run build",
    "synth": "npm run build:frontend && cd infra && cdk synth",
    "deploy": "npm run build:frontend && cd infra && cdk deploy",
    "destroy": "cd infra && cdk destroy --force"
  }
}